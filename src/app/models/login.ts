export class Login {
    uid:number;
    username: String;
    password: String;

    constructor(uid,username,password)
    {
            this.uid=uid;
            this.username=username;
            this.password=password;
    }
}
